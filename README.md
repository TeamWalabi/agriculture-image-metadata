# agriculture-image-metadata
Linked Data–based metadata model for agricultural image data. Defines how images, observations, and contextual metadata reference existing ontologies and codelists, with a minimal extension layer for project‑specific elements.
