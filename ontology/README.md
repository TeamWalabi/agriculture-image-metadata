# Ontology

This folder contains the core ontology file:

- `ontology.ttl`

## Hosting

The canonical namespace for this project is `https://w3id.org/agri-image/`.

The ontology file (`ontology.ttl`) will be hosted at `https://w3id.org/agri-image`.


